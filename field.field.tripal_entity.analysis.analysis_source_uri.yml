uuid: 469a722e-dd72-4d43-8aca-f58661a78dfc
langcode: en
status: true
dependencies:
  config:
    - field.storage.tripal_entity.analysis_source_uri
    - tripal.content_type.analysis
  module:
    - tripal_chado
id: tripal_entity.analysis.analysis_source_uri
field_name: analysis_source_uri
entity_type: tripal_entity
bundle: analysis
label: 'Data Source URI'
description: 'The URI (e.g. web URL) where the source data can be retrieved. Ideally this URL will be a permanent URL or DOI but if that is not available, try to make this URL as specific as possible. For example, use the URL to the page describing this exact dataset rather then just a generic URL to the resource hosting it or a download page hosting multiple datasets.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  termIdSpace: data
  termAccession: '1047'
  debug: false
field_type: chado_text_type_default
