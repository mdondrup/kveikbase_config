uuid: 06472dc2-f1e2-4d29-9bbc-9bc91b0cacdb
langcode: en
status: true
dependencies:
  config:
    - field.storage.tripal_entity.germplasm_variety_relationship
    - tripal.content_type.germplasm_variety
  module:
    - tripal_chado
id: tripal_entity.germplasm_variety.germplasm_variety_relationship
field_name: germplasm_variety_relationship
entity_type: tripal_entity
bundle: germplasm_variety
label: Relationship
description: "Germplasm related to this one. Think of relationship statements as sentences: SUBJECT TYPE OBJECT (e.g. 'Sara (subject) is the mother of (type) Jacob (object)'). By default the current germplasm is the subject; chose Reverse if the object is the current germplasm."
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  termIdSpace: SBO
  termAccession: '0000374'
  debug: false
field_type: chado_relationship_type_default
